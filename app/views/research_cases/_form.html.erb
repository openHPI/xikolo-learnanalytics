<%= form_for(@research_case) do |f| %>
  <% if @research_case.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@research_case.errors.count, "error") %> prohibited this research_case from being saved:</h2>

      <ul>
      <% @research_case.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
