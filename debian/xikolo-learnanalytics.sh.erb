#!/bin/sh
<%
  require 'shellwords'
%>

export RAILS_ENV=${RAILS_ENV:-production}
export GEM_PATH=<%= gempath %>
export DEB_VERSION=<%= version %>

cd <%= Shellwords.escape(prefix) %>

exec <%= Shellwords.escape(File.join(prefix, 'bin')) %>/$@